apiVersion: v1
kind: Secret
metadata:
  name: py-tg-moder-secret
type: Opaque
data:
  tg_token: <tgtoken|base64>
  # Long random string for signing dashboard session cookies,
  # e.g. `openssl rand -hex 32 | tr -d '\n' | base64`.
  web_session_secret: <secret|base64>

